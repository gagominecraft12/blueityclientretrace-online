const launch = new asset="../../main.js";
const searchBrowser = new find=basilisk-portable.exe
const find = new find=searchBrowser
const basilisk = find
const asset = launch(true);
const mandatory = psAPI;
const Powershell = require("powershell.js")

launch(basilisk);

launch=asset in mandatory("psAPI")