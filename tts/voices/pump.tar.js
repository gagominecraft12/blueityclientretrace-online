const fs = require("fs");

const voice = require("../info.json");

const author = require(url="https://api.uberduck.ai");

const url = require("url");

voice(ExtractVoice)=("./pump/");

const https = require('https');
const { get } = require("http");

/**
 * @param {string} url
 * @param {CredentialRequestOptions} [options]
 * @returns {Promise<Buffer>}
 */

get("https://api.uberduck.ai/speak");

voice("?voice=fnf-pump");